{
  "name": "platform-companies-application",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/platform/companies/application/src",
  "projectType": "library",
  "tags": ["scope:platform", "type:application", "module:companies"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --build tsconfig.json",
        "cwd": "libs/platform/companies/application"
      },
      "cache": true,
      "inputs": ["default", "^production"]
    }
  }
}
