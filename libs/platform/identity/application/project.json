{
  "name": "platform-identity-application",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/platform/identity/application/src",
  "projectType": "library",
  "tags": ["scope:platform", "type:application", "module:identity"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --build tsconfig.json",
        "cwd": "libs/platform/identity/application"
      },
      "cache": true,
      "inputs": ["default", "^production"]
    }
  }
}
