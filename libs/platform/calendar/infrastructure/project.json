{
  "name": "platform-calendar-infrastructure",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/platform/calendar/infrastructure/src",
  "projectType": "library",
  "tags": ["scope:platform", "type:infrastructure", "module:calendar"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --build tsconfig.json",
        "cwd": "libs/platform/calendar/infrastructure"
      },
      "cache": true,
      "inputs": ["default", "^production"]
    }
  }
}
