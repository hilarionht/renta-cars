{
  "name": "platform-calendar-domain",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/platform/calendar/domain/src",
  "projectType": "library",
  "tags": ["scope:platform", "type:domain", "module:calendar"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --build tsconfig.json",
        "cwd": "libs/platform/calendar/domain"
      },
      "cache": true,
      "inputs": ["default", "^production"]
    }
  }
}
