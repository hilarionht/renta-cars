{
  "name": "platform-calendar-application",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/platform/calendar/application/src",
  "projectType": "library",
  "tags": ["scope:platform", "type:application", "module:calendar"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --build tsconfig.json",
        "cwd": "libs/platform/calendar/application"
      },
      "cache": true,
      "inputs": ["default", "^production"]
    }
  }
}
