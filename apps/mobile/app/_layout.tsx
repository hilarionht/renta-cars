import { Stack } from 'expo-router';

// Expo Router (docs/06-CONVENCIONES-FRONTEND.md SS4) - layout raiz, sin logica de negocio
// todavia (Identity no existe hasta Fase 0).
export default function RootLayout() {
  return <Stack />;
}
