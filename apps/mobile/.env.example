# Sin secretos - copiar a `.env`. Expo/Metro carga este archivo y expone las variables
# `EXPO_PUBLIC_*` al bundle de cliente (equivalente a `NEXT_PUBLIC_*` en web-admin).
EXPO_PUBLIC_API_URL=http://localhost:3000
