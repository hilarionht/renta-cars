# docs/engineering/07-CONFIGURATION.md §3: `.env.local` (convención de Next.js) para
# variables exclusivas de frontend (`NEXT_PUBLIC_*`). Sin secretos - copiar a `.env.local`.
NEXT_PUBLIC_API_URL=http://localhost:3000
