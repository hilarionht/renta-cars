// Pagina raiz minima del bootstrap (docs/engineering/10-BOOTSTRAP-PLAN.md, paso 13) - sin
// logica de negocio real todavia (Identity no existe hasta Fase 0). La primera feature de
// prueba real vive en app/(admin)/login/, generada con `generate:feature`.
export default function Index() {
  return (
    <main>
      <h1>Renta - Administración</h1>
    </main>
  );
}
